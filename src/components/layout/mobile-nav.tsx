
'use client';

import * as React from 'react';
// This component is no longer used in the header, as navigation is handled by the new persistent sidebar.
// It is kept in case it's needed elsewhere.

export function MobileNav() {
  return null;
}
